{
  "name": "@planet-matrix/seibun",
  "version": "0.2.2",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel --filter=@planet-matrix/* run dev",
    "build": "pnpm -r --filter=@planet-matrix/* run build",
    "format": "prettier --cache --write .",
    "release": "bumpp -r",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "prettier": "@planet-matrix/prettier-config",
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.1.1",
    "@planet-matrix/prettier-config": "^0.2.2",
    "@types/node": "^20.8.7",
    "bumpp": "^9.2.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2",
    "vitepress": "1.0.0-rc.23"
  },
  "packageManager": "pnpm@8.9.2"
}
