{
  "name": "@planet-matrix/seibun",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter=@planet-matrix/* run build",
    "format": "prettier --cache --write .",
    "release": "bumpp -r"
  },
  "prettier": "@planet-matrix/prettier-config",
  "devDependencies": {
    "@planet-matrix/prettier-config": "^0.2.0",
    "@types/node": "^20.8.5",
    "bumpp": "^9.2.0",
    "prettier": "^3.0.3",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  },
  "packageManager": "pnpm@8.9.0"
}
